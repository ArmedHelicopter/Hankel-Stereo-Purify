# Global Engineering Guidelines
本项目的所有规划、设计和代码实现，必须严格遵循以下全局工程标准：

1. **核心设计哲学**：
   - 贯彻 SOLID、DRY（不要重复自己）和 KISS（保持简单）原则。
   - 追求高内聚、低耦合的架构设计。
2. **零容忍底线**：
   - 绝不吞没异常（No Silent Failures）：任何潜在的错误、越界或 I/O 异常必须被显式处理或抛出。
   - 绝不硬编码敏感信息：所有 Token、密钥必须通过环境变量或配置注入。
3. **安全与健壮性优先**：
   - 默认假设所有外部输入都是不可信的，必须进行严格校验。
   - 在任何涉及状态变更的操作中，必须考虑并发竞态条件（Race Conditions）和数据一致性。