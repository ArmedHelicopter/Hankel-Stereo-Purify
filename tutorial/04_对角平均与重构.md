# 第 4 章：对角平均与重构

## 直觉先行

SVD 截断后的矩阵**一般不再是严格 Hankel**（反对角线不再全相等）。不能直接「按行读回」一维序列。做法是：对每条反对角线上的元素**取平均**，映射回长度约 \(L+K-1\) 的序列——这叫对角平均（diagonal averaging）。左右子块各做一遍，再 `column_stack` 成立体声输出帧。

类比：拼图碎块对不齐，就按重叠区域取平均抹平接缝。

## 决策复盘（Why vs Why not）

**为什么用 `t=i+j` + `numpy.bincount` 聚合？**  
对固定形状 `(m,n)` 预计算 `t_flat`（每个矩阵元一条 \(t=i+j\)），用 `bincount(weights=...)` 一次完成按反对角求和，再除以各 \(t\) 上元素个数。相比为整批张量构造 **`O(B·mn)`** 长度的 scatter 索引（`repeat`/`tile` + `add.at`），当前实现**控制临时整型数组规模**；立体声等对 batch 维仅做小 **`B` 的 Python 循环**（见 `batched_diagonal_average`）。

**为什么不直接在频域做 IFFT？**  
本项目 MSSA 路径在时域 Hankel-SVD-对角平均闭环；换域等于换算法，超出当前仓库范围。

### 最小公式（抄笔记）

- 单声道块对角平均输出序列长 **\(N=L+K-1\)**（与 [00](00_架构总览.md) 符号表一致）。
- 立体声：左右各得到长度 \(N\) 的一维序列，再 `column_stack` 为 **`(N,2)`**。

### 代码锚点

| 定位 | 路径 |
|------|------|
| `_t_flat_and_cnt`、`_plane_diagonal_average`、`diagonal_reconstruct`、`fast_diagonal_average` | [`src/core/stages/d_diagonal.py`](../src/core/stages/d_diagonal.py) |
| Golden 与形状 | [`tests/test_d_diagonal.py`](../tests/test_d_diagonal.py) |

## 思维挂钩

| 代码 | 专业课 | 软件工程 |
|------|--------|----------|
| `bincount` 按 `t=i+j` 加权 | 加权平均、Hankel 逆嵌入 | 热路径：向量化聚合 + 小 `B` 循环 |
| `diagonal_reconstruct` 内 `stack` 后 `batched_diagonal_average` | 分块对角操作 | 左右一次 batch，避免重复建 `t_flat` |

## 晦涩点与建议

- **晦涩**：`joint` 列数为偶数、左半右半各 \(K\) 列，读代码时要自己画列索引。  
- **建议**：对照 [`test_d_diagonal.py`](../tests/test_d_diagonal.py) 里「恢复原始一维序列」的 golden 检验。

**下一章**：单帧输出如何拼成整文件——OLA 与门面。
